#ifndef SONWINDOW_H
#define SONWINDOW_H

#include <QDialog>

class sonwindow : public QDialog
{
    Q_OBJECT
public:
    explicit sonwindow(QWidget *parent = nullptr);

signals:
};

#endif // SONWINDOW_H
